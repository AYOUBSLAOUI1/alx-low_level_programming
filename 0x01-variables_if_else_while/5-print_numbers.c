#include <stdio.h>
/**
 * main - printing 10 numbers.
 * Description : program that prints 10 numbers.
 * Return: Always 0 (Success).
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
